shared_example_for "feed_elements" do
  it "populates Page fields included in an elements_set and responded to a :set with a test data" do
  end

  it "is invoked with an optional hash which tells what elements to fill with what data" do
  end

  it "can be invoked with no arguments causing fields to be filled with default data" do
  end
end

