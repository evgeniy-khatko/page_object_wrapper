require 'spec_helper'

describe PageObjectWrapper do
  describe "define_page_object" do
    it "accepts one argument - page_object label" do
    end

    it "defines a PageObject with a label, locator and optional identifier" do
    end

    it "can define PageObject element with labels and locators" do
    end

    it "can define PageObject element-set with a label" do
    end

    it "can define a PageObject table whith a label and locator" do
    end

    it "can define a PageObject pagination with a label and locator" do
    end

    it "can define a PageObject action with a label and new_page_object" do
    end
  end
end
