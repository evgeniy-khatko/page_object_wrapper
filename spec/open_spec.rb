require 'spec_helper'

describe "PageObjectWrapper.open" do
  it "accepts one argument - :label of the page to open" do
  end

  it "raises Watir::ElementNotFound if a page object with provided label has not been found" do
  end

  it "navigates current browser to the specified PageObject instance locator (url)" do
  end

  it "checks each uniq_element existance after page is opened" do
    it "raises Watir::ElementNotFound if any of the uniq_elements has not been found" do
    end
  end

  it "returns opened PageObject instance" do
  end
end

